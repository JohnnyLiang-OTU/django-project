// document.addEventListener('DOMContentLoaded', function() {
    // const brandContainer = document.querySelector('.brand_container');
    // const firstCard = document.querySelector('.brand_card');
    // const buttonWidth = document.querySelector('.prev_button').offsetWidth;
    // brandContainer.scrollLeft = (firstCard.offsetWidth) - buttonWidth;

    // const nextBtn = document.querySelector('.next_button');
    // const prevBtn = document.querySelector('.prev_button');

    // let containerDimension = brandContainer.getBoundingClientRect();
    // let containerWidth = containerDimension.width;
        
    // nextBtn.addEventListener('click', () => {
    //     if (brandContainer.scrollLeft + containerWidth >= brandContainer.scrollWidth) {
    //         brandContainer.scrollLeft = 0;
    //     } else {
    //         brandContainer.scrollLeft += containerWidth;
    //     }
    // });

    // prevBtn.addEventListener('click', () => {
    //     if (brandContainer.scrollLeft == 0) {
    //         brandContainer.scrollLeft = brandContainer.scrollWidth;
    //     } else {
    //         brandContainer.scrollLeft -= containerWidth;
    //     }
    // });
// });